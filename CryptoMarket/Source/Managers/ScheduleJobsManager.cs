namespace CryptoMarket.Source.Managers{
    public class ScheduleJobsManager{
        //public class DepositCheckerJob: IJob{
        //    public void Execute(IJobExecutionContext jobContext){
        //        using (var context = new ApplicationDbContext()){
        //            var allCoinSystems = context.CoinSystems.ToList();
        //            foreach (var coinSystem in allCoinSystems){
        //                var rpc = CoinsRpcManager.Init(coinSystem.Id.ToString());

        //                var latestTransactions = rpc.ListTransactions(Count: 256);
        //            }
        //        }
        //        throw new NotImplementedException();
        //    }
        //}
    }
}